APP_ENV=test
APP_SECRET=change_me
DATABASE_URL=postgresql://invoicing_user:invoicing_pass@postgres:5432/invoicing_test_db?serverVersion=16&charset=utf8
MESSENGER_TRANSPORT_DSN=sync://

# Optional: test database URL used by CI/tests if needed
#DATABASE_URL_TEST="postgresql://invoicing_user:invoicing_pass@postgres:5432/invoicing_test_db?serverVersion=16&charset=utf8"

## Copy this file locally to `.env.test` before running acceptance tests:
# cp .env.test.dist .env.test
